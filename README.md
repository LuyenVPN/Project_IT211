# Hospital Management Backend

Spring Boot REST backend for hospital management (JWT auth, appointments, medical records, audit logging).

## Run

```cmd
gradlew.bat bootRun
```

## Test

```cmd
gradlew.bat test
```

## Test profile

Integration tests use H2 in-memory database via `src/test/resources/application.properties`.

